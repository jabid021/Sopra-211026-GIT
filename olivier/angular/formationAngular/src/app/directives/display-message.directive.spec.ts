import { DisplayMessageDirective } from './display-message.directive';

describe('DisplayMessageDirective', () => {
  it('should create an instance', () => {
    const directive = new DisplayMessageDirective();
    expect(directive).toBeTruthy();
  });
});
